@model IEnumerable<PlanificatorProiecte.Models.Domain.StareAlocare>
@{}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <a class="btn btn-dark" href="/StareAlocare/Add">Adauga stare</a>
        </div>
    </div>
    <div class="row">
        <div class="col-md-8">
            <h3>Stari alocare</h3>

            <table class="table table-stripped table-bordered table-dark">
                <thead>
                    <tr>
                        <th style=" width:20%">Denumire stare</th>
                       
                        <th style=" width:25%">Actiune</th>
                    </tr>
                </thead>

                <tbody>
                @foreach(var item in Model)
                {
                    <tr>
                        <td>@item.NumeStareAlocare</td>

                        <td>
                            
                             <a class="btn btn-success float-left" href="/StareAlocare/Update?id=@item.Id">Modifica</a> |
                             <a class="btn btn-danger float-right" onClick="return window.confirm('Doriti sa stergeti inregistrarea?')" href="/StareAlocare/Delete?id=@item.Id">Sterge</a>
                        </td>
                          


                    </tr>
                }
                </tbody>
            </table>

            


        </div>

    </div>
</div>

